name: Apply final mobile polish

on:
  push:
    branches:
      - codex/hybrid-best-version-20260724
    paths:
      - scripts/apply-final-mobile-polish.mjs
      - .github/workflows/apply-final-mobile-polish.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/hybrid-best-version-20260724
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/apply-final-mobile-polish.mjs
      - name: Commit final mobile polish
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.tsx
          git commit -m "Polish mobile typography, forms and modal behaviour"
          git push origin HEAD:codex/hybrid-best-version-20260724
